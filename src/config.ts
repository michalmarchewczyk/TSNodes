const defaultConfig = {
    defaultCanvasWidth: 4000,
    defaultCanvasHeight: 4000,
    defaultNodeWidth: 120,
    minNodeWidth: 100,
    maxNodeWidth: 1240,
    debugOutline: true,
}

const config = {
    ...defaultConfig,
}

export default config;
