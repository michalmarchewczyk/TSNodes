const defaultConfig = {
    defaultCanvasWidth: 4000,
    defaultCanvasHeight: 4000,
    defaultNodeWidth: 120,
    minNodeWidth: 100,
    maxNodeWidth: 240,
    debug: true,
}

const config = {
    ...defaultConfig,
}

export default config;
